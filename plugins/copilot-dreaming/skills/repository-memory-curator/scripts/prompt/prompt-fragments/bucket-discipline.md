Your bucket is one **topical slice of the full memory list, handed to you for parallel processing.** The grouping is a best-effort clustering by subject, **not** an authoritative judgment, so a record may be imperfectly placed and its bucket membership carries no signal you should act on: **never remove a record because it seems off-topic or out of place in your bucket**. Later stages dedup across all buckets, but records are never re-bucketed, so dropping one here permanently deletes it from the final list of repo memories. Cut a record only for a substantive reason from the checklist below (wrong/stale, duplicate, obvious, low-value). The orchestrator audits your drop log for spurious removals, so **record every removal with its specific reason**.
