Do **not** call `store_memory`, `vote_memory`, write to any memory database/API, post issue/PR comments, request reviews, push commits, or write anywhere outside the output paths explicitly assigned in your task prompt. The repository checkout (especially its root) must stay **pristine** — never write into it, and read it with `git -C <repo-root> …` or absolute paths rather than `cd`-ing in to write. Read only the inputs named in your task prompt (plus the checkout and any sources you must verify against); do not read other units' scratch or output files. This skill emits JSONL artifacts only; applying memories is out of scope.

Your shell's working directory may be the live checkout, so a **bare relative path** (e.g. `gh api … > projects.json`) would land in the checkout root. Write every scratch or intermediate file (e.g. to hold `gh`/query output) **only** under the absolute scratch directory given in your task prompt — either as an explicit absolute path inside it, or via `mktemp`/`mktemp -d` created inside it (e.g. `mktemp "<scratch-dir>/out.XXXXXX"`): always an **absolute** path, **never** a bare relative one, never inside the checkout, and never a shared or fixed/global temp path (e.g. a hardcoded `/tmp/out.json`) that a sibling subagent or a concurrent run could overwrite.
